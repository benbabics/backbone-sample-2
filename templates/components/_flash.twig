{% if notice %}

  <div class="flash flash-{{ notice.type | default('info') }}">
    {{ notice.text }}

    {% if should_hide_close %}
      <a href="" class="close">&times;</a>
    {% endif %}
  </div>

{% endif %}